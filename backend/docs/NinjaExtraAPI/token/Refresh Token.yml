info:
  name: Refresh Token
  type: http
  seq: 2
  tags:
    - token

http:
  method: POST
  url: "{{baseUrl}}/api/token/refresh"
  body:
    type: json
    data: |-
      {
        "refresh": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/token/refresh"
      method: POST
      body:
        type: json
        data: |-
          {
            "refresh": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "refresh": "",
            "access": ""
          }
