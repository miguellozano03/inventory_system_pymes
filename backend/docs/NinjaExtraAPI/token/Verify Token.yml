info:
  name: Verify Token
  type: http
  seq: 3
  tags:
    - token

http:
  method: POST
  url: "{{baseUrl}}/api/token/verify"
  body:
    type: json
    data: |-
      {
        "token": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/token/verify"
      method: POST
      body:
        type: json
        data: |-
          {
            "token": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: "{}"
