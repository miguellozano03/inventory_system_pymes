info:
  name: Delete Product
  type: http
  seq: 5
  tags:
    - Inventory_-_Products

http:
  method: DELETE
  url: "{{baseUrl}}/api/inventory/products/:product_id"
  params:
    - name: product_id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 204 Response
    description: No Content
    request:
      url: "{{baseUrl}}/api/inventory/products/:product_id"
      method: DELETE
      params:
        - name: product_id
          value: ""
          type: path
    response:
      status: 204
      statusText: No Content
      body:
        type: text
        data: ""
