info:
  name: Delete Supplier
  type: http
  seq: 5
  tags:
    - Inventory_-_Suppliers

http:
  method: DELETE
  url: "{{baseUrl}}/api/contacts/suppliers/:supplier_id"
  params:
    - name: supplier_id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 204 Response
    description: No Content
    request:
      url: "{{baseUrl}}/api/contacts/suppliers/:supplier_id"
      method: DELETE
      params:
        - name: supplier_id
          value: ""
          type: path
    response:
      status: 204
      statusText: No Content
      body:
        type: text
        data: ""
