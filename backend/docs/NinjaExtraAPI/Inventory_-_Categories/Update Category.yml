info:
  name: Update Category
  type: http
  seq: 4
  tags:
    - Inventory_-_Categories

http:
  method: PATCH
  url: "{{baseUrl}}/api/inventory/categories/:category_id"
  params:
    - name: category_id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "name": "",
        "description": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/inventory/categories/:category_id"
      method: PATCH
      params:
        - name: category_id
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "name": "",
            "description": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "id": "",
            "company_id": "",
            "name": "",
            "description": ""
          }
